users:
  table:
    no_users: 'No users found'
    head:
      username: Username
      role: Role
      state: State
      actions: Actions
    body:
      actions:
        view: View User Profile
        edit: Edit User
        delete: Delete User
        impersonate: Impersonate User
