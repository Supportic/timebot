# Enable stateless CSRF protection for forms and logins/logouts
# https://symfony.com/doc/7.4/reference/configuration/framework.html#csrf-protection
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - authenticate
            - logout
