twig:
  file_name_pattern: "*.twig"
  globals:
    switch_user_parameter: '%app.switch_user.parameter%'
    seo_title_portal: '%app.seo.title.portal%'
    seo_title_separator: '%app.seo.title.separator%'

when@test:
  twig:
    strict_variables: true
